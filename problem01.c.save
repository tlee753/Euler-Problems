#include <stdio.h>
#include <time.h>
#include <math.h>

int main() {
    
    int total = 0;
    int i;
    for (i=0; i<1000; i++) {
        if ( (i%3==0) || (i%5==0) ) {
            total += i;
        }
    }
    printf("%d\n", total);
    return 0;
}
